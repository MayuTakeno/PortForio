<div class="container" >
  <div class="row">
    <div calss="col-md-2" style="text-align: center;">
      <h4>注文履歴</h4>
    </div>
    <div class="col-md-1"></div>
    <div class="col-md-7">
      <table class="table" style="table-layout: fixed; word-wrap: break-wrap;">
        <thead>
          <tr>
            <th>発注日</th>
            <th>受取方法</th>
            <th>製品名</th>
            <th>請求額</th>
            <th>配送状況</th>
            <th>注文詳細</th>
          </tr>
        </thead>
        <tbody>
          <% @orders.each do |order| %>
          <tr>
            <td><%= order.created_at.strftime("%Y/%m/%d %H:%M") %></td>
            <td><%= order.payment_method_i18n %></td>
            <td>
              <% order.product_orders.each do |product_order| %>
                <%= image_tag product_order.product.image, size: "25x25" %><br>
                <%= product_order.product.name %>
              <% end %>
            </td>
          <td><%= order.billing_amount %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>