<!--記事詳細ページ-->

<div class="container">
  <div class="row">
    <!--見出し-->
    <div class="col-md-2" style="text-align: center;">
      <h4>記事詳細</h4>
    </div>
    <!--内容表示-->
    <div class="col-md-8" style="text-align: center; margin-bottom: 30px;">
      <!--画像-->
      <%= image_tag @notice.get_image, size: "300x300" %>
      <br>
      <br>
      <!--title-->
      <strong><%= @notice.title %></strong>
      <br>
      <br>
      <!--body-->
      <div style="border-top: 1px solid #000000;">
        <br>
        <h6><%= simple_format @notice.body %></h6>
      </div>
      <!--caption-->
      <div>
        <br>
        <span>※<%= simple_format @notice.caption %></span>
      </div>
    </div>
    <!--編集/削除リンク-->
    <div class="col-md-2" style="text-align: center; margin-bottom: 30px;">
      <% if admin_signed_in?%>
        <%= link_to edit_admin_notice_path(@notice.id), class: 'btn btn-sm btn-success' do %>
          編集
        <% end %>
        <br>
        <br>
        <%= link_to admin_notice_path(@notice.id), method: :delete, class: 'btn btn-sm btn-danger' do %>
          削除
        <% end %>
      <% end %>
    </div>
  </div>
</div>